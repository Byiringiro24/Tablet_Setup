# Node
node_modules
credentials.md/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js
frontend/.next/
frontend/out/

# Build outputs
FKBridge/bin/
FKBridge/obj/
DLLs/

# Environment & secrets
.env
.env.local
.env*.local

# OS
.DS_Store
Thumbs.db

# Data (runtime generated) — these files are local to each tablet
# git pull will NEVER overwrite them
backend/data/attendance.json
backend/data/attendance-settings.json
backend/data/device-config.json
backend/data/students.json
backend/data/wireguard-public.key

# WireGuard private keys — NEVER commit these
backend/data/wireguard-private.key
credentials.md
